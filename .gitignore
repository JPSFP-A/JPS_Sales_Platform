.vercel

# Secrets — never commit
.env
.env.local

# --- excluded from commit (raw customer data + scratch + backups) ---
analysis/
salesanalysis_deploy/
*.pre-submit.bak.html

# Supabase CLI local cache
supabase/.temp/
